apiVersion: v1
kind: Service
metadata:
  name: {{ include "pg-mcp-server.fullname" . }}
  labels:
    {{- include "pg-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pg-mcp-server.selectorLabels" . | nindent 4 }}
